# VaaniWeb Environment Variables
# Copy this file to .env.local and fill in your actual values
# NEVER commit .env.local to git!

# MongoDB Database Connection
# Get a free cluster from: https://www.mongodb.com/cloud/atlas
MONGODB_URI=mongodb+srv://YOUR_USERNAME:YOUR_PASSWORD@YOUR_CLUSTER.mongodb.net/YOUR_DATABASE?retryWrites=true&w=majority

# JWT Secret for Authentication (minimum 32 characters recommended)
# Generate a secure key using: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your_secure_random_jwt_secret_key_here_minimum_32_characters

# Groq AI API Key (Free tier available)
# Sign up at: https://console.groq.com
GROQ_API_KEY=gsk_YOUR_GROQ_API_KEY_HERE

# Application URL
NEXT_PUBLIC_ROOT_URL=http://localhost:3000

# Google Search Console site verification token (optional but recommended)
# After creating a property in Search Console, copy the verification code value
# from the HTML tag method (the "content" attribute) and paste it below.
NEXT_PUBLIC_GOOGLE_SITE_VERIFICATION=

# Google OAuth (Optional - for Google Sign-In)
# Get credentials from: https://console.cloud.google.com
NEXT_PUBLIC_GOOGLE_CLIENT_ID=your_google_client_id_here.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your_google_client_secret_here

# Resend Email Service (Required for email verification)
# Get FREE API key from: https://resend.com/api-keys
# 100 emails/day free tier - no credit card required
RESEND_API_KEY=re_YOUR_RESEND_API_KEY_HERE
